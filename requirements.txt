# Hand-maintained minimal dependency set (floors only, no exact pins).
#
# Runtime deps scanned from actual imports across the repo:
#   torch / numpy           - models, training, inference
#   flask                   - demo_analysis/web_app.py, scripts/annotator
#   demoparser2 (+ polars)  - demo parsing (demoparser2 returns polars DataFrames)
#   webdataset / zstandard  - dataset/model3_wds_space_only.py, data/create_training_data_*
#   python-snappy           - demoparser_utils/state_extract.py (optional compressed JSON output)
#   tqdm                    - progress bars in data/training/inference scripts
#   wandb                   - training logging
#   agent-framework-openai  - demo_analysis/llm_summary.py (OpenAI-compatible LLM client)
#   huggingface_hub         - scripts/download_model.py, scripts/download_data.py
#   PyYAML                  - configs (model/training/tokenizer/map knowledge)
torch>=2.2
numpy>=1.26
flask>=3.0
demoparser2>=0.41
polars>=1.0
webdataset>=0.2
zstandard>=0.22
python-snappy>=0.7
tqdm>=4.66
wandb>=0.19
agent-framework-openai>=1.2
huggingface_hub>=0.30
PyYAML>=6.0

# Test-only dependencies (used by CI): pytest
